version: 2

sources:
  - name: raw_data
    description: "Raw data from the NYC TLC"
    database: taxi_rides_ny  # In BigQuery, this is the project name
    schema: prod             # Dataset name
    tables:
      - name: yellow_tripdata  # Table name
      - name: green_tripdata